import os

def ingest_data(source_dir):
    print(f"Ingesting data from {source_dir}...")
    # Logic to read raw data and move to processed
    pass

if __name__ == "__main__":
    ingest_data("./data/raw")
